# Porta do servidor (padrão: 3001)
PORT=3001

# Origens CORS permitidas (separadas por vírgula)
CORS_ORIGINS=http://localhost:3000,http://localhost:3002,http://localhost:5173

# Diretório para salvar autenticação do WhatsApp (padrão: auth_info_baileys)
AUTH_STATE_DIR=auth_info_baileys

# Delay base entre mensagens em segundos (padrão: 3)
# Valores válidos: 0-300
DELAY_SECONDS=3

# Ambiente (development | production)
NODE_ENV=development
